#include<iostream>

using namespace std;

int main() {

    int n;
    long long int x, y;

    cin >> n;

    while(n--){
        cin >> x >> y;

        x = x|1;

        //PA
         cout << y * (x + (x + 2 * (y-1)))/2 << endl;
    }
    
return 0;
}